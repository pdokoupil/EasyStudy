__pycache__
.DS_Store
*.pyc
*.pckl
*.csv
*.npy
flask_session
server/cache/
server/instance/
*.sqlite
img
cache

# Datasets & their images are fetched via scripts/fetch_data.py, never committed
# (they used to be git-LFS tracked, which caused clone-time quota errors).
*.zip
server/static/datasets/**/img/
.venv/
venv/
.ruff_cache/
.pytest_cache/
# Local, private notes/scripts (publishing creds/workflow) — never committed
*.local.md
*.local.sh

# Hypothesis property-testing cache
.hypothesis/

# Build/uv artifacts
*.egg-info/
.cache/

# RecBole training artifacts (should stay confined to its per-fit tempdir, but ignore as a
# defense-in-depth in case something writes them to the working directory anyway).
log_tensorboard/
/log/
/saved/
